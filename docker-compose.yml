version: '3.8'

services:
  redis:
    image: redis:latest
    container_name: hostelhub-redis
    ports:
      - "16379:6379"
    restart: always
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes

volumes:
  redis_data:
