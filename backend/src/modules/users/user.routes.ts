import { Router } from 'express'

const router = Router()

// TODO: Add user routes
// GET / (list all)
// GET /:id (get single)
// POST / (create)
// PUT /:id (update)
// DELETE /:id (delete)

export default router
