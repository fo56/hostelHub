import { Router } from 'express'

const router = Router()

// TODO: Add mess routes
// GET /menu (current menu)
// GET /menu/:date (menu by date)
// POST /menu (create menu - admin)
// PUT /menu/:id (update menu - admin)
// GET /dues (mess dues)

export default router
