import { Router } from 'express'

const router = Router()

// TODO: Add issue routes
// GET / (list all)
// GET /:id (get single)
// POST / (create)
// PUT /:id (update)
// DELETE /:id (delete)
// GET /assigned (for workers)

export default router
