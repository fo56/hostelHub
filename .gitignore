# Dependencies (can be reinstalled with npm install)
node_modules/
package-lock.json

# Environment variables (contain passwords, API keys - NEVER commit these!)
.env
.env.local


# Build outputs (generated files with npm run build)
dist/
build/

.DS_Store
